apply plugin: 'groovy'
apply plugin: 'eclipse'

def versioner = new com.sarhan.versioner.Versioner()

group = 'com.sarhanm'
version = versioner.getVersion()

println "Building version: $project.version"

dependencies {
    compile gradleApi()
    compile localGroovy()
}
